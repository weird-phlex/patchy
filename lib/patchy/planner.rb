# frozen_string_literal: true

module Patchy
  class Planner
    # Not exactly sure, whether code will go into Main or Planner.
    # The project is still small, so keeping everything in Main makes
    # it easier to shuffle stuff around for now.
  end
end
